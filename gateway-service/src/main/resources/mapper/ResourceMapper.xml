<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "<http://mybatis.org/dtd/mybatis-3-mapper.dtd>">
<mapper namespace="com.lee.dao.ResourceMapper">


    <select id="getResourcesByUserId" resultType="com.lee.domain.Resource">
        select a.* from sys_resource a
        left join sys_role_resource_relation b
        on a.id = b.resource_id
        left join sys_user_role_relation c
        on b.role_id = c.role_id
        where c.user_id = #{id};
    </select>
</mapper>